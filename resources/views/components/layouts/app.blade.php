@props(['title' => 'Koqoi Slides', 'stage' => false])
<!doctype html><html lang="es"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><meta name="csrf-token" content="{{ csrf_token() }}"><title>{{ $title }}</title><link rel="stylesheet" href="/app.css"><link rel="stylesheet" href="/audience.css"><link rel="stylesheet" href="/identity.css"></head>
<body class="{{ $stage ? 'stage-body' : '' }}">@unless($stage)<nav><a class="brand" href="/"><i>K</i> Koqoi<span>Slides</span></a><div class="navlinks">@auth<a href="{{ route('presentations.index') }}">Mis presentaciones</a><form method="post" action="{{ route('logout') }}">@csrf<button class="link">Salir</button></form>@else<a href="{{ route('login') }}">Ingresar</a><a class="btn small" href="{{ route('register') }}">Crear cuenta</a>@endauth</div></nav>@endunless
<main>@if(session('ok'))<div class="flash ok">{{ session('ok') }}</div>@endif @if(session('answered'))<div class="flash ok">{{ session('answered') }}</div>@endif @if($errors->any())<div class="flash error">{{ $errors->first() }}</div>@endif {{ $slot }}</main>
@unless($stage)<footer>Koqoi Slides · Presentaciones que escuchan a tu audiencia</footer>@endunless</body></html>
